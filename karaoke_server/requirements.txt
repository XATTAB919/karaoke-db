flask                  # фреймворк для запуска API-сервера
flask-cors             # разрешение запросов с клиента (например, с твоего приложения на Kivy)
psycopg2-binary        # драйвер для подключения к PostgreSQL
bcrypt                 # хеширование паролей (защита от взлома)
python-dotenv          # удобное чтение переменных из .env (пока не используем, но можно)